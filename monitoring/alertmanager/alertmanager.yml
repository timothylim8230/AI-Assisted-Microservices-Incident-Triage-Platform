route:
  receiver: "webhook-receiver"
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 1m

receivers:
  - name: "webhook-receiver"
    webhook_configs:
      - url: "http://host.docker.internal:8000/alerts/webhook"
        send_resolved: true


  